# Optional — app works fully offline without this key (Leaflet/OSM fallback)
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Future backend URL (not required for pilot)
# VITE_API_BASE_URL=https://api.safeher.app
